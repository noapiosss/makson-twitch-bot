@model IList<Contracts.Database.Command>

@{
    ViewData["Title"] = "Commands";
}

<link rel="stylesheet" href="~/css/social-medias-styles.css" />

<div class="text-center">
    <h1 class="display-4">Social Medias</h1>
</div>

<table class="table" id="table">
    <thead>
        <tr>
            <th class="command-column">
                Command
            </th>
            <th class="output-column">
                Link
            </th>
            <th class="button-column">

            </th>
        </tr>
    </thead>

    <tbody id="table-body">
        @foreach (var socialMedia in Model)
        {
            <tr id="row-@socialMedia.CommandName">
                <th>
                    @socialMedia.CommandName
                </th>
                <th>
                    @socialMedia.CommandOutput
                </th>
                <th>
                    <div class="delete-button" id="@socialMedia.CommandName">delete</div>
                </th>
            </tr>
        }
        <tr id="add-row">
            <th>
                <input type="text" class="command-input" id="command-input" placeholder="command">
            </th>
            <th>
                <input type="text" class="output-input" id="output-input" placeholder="output">
            </th>
            <th>
                <div class="add-button" id="add-button">add</div>
            </th>
        </tr>
    </tbody>
</table>

<script src="~/js/social-medias-page.js"></script>